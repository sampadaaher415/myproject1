name: Deploy

on:
  workflow_dispatch:
  push:
     branches: [ main]
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: echo "Deployed"
