name: Deploy
on:
   workflow_dispatch: 
   push:
      branches: [main]
  jobs:
    deploy:
      runs-on: ubantu-latest
      steps:
        - uses: actions/checkout@4
        - run: echo "Deployed"
 
